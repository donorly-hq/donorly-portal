# Backend base URL used by the server-side API proxy (src/app/api/[...path]/route.ts).
# Copy this file to .env.local and adjust. The browser never talks to the backend directly.
BACKEND_URL=http://localhost:8080
